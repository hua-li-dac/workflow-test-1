name: Envioments

on:
  workflow_dispatch:

jobs:
  DEV:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy
        run: echo "Deployed to DEV"

  STAGE:
    runs-on: ubuntu-latest

    needs: DEV
    steps:
      - name: Deploy
        run: echo "Deployed to Stage"

  PROD:
    runs-on: ubuntu-latest

    needs: STAGE
    steps:
      - name: Deploy
        run: echo "Deployed to Prod"  